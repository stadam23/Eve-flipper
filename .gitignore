# Binaries
*.exe
*.exe~
*.dll
*.so
*.dylib

# Build output
/build/
/tmp/

# Local clone of GitHub wiki (for editing wiki from repo)
wiki-repo/

# Old docs/wiki markdown dump + local notes (do not track in repo)
docs/

# Local marketing landing (work in progress, do not track yet)
landing/

# SDE data cache (large, auto-downloaded)
/data/

# SQLite database files
flipper.db
flipper.db-shm
flipper.db-wal

# Legacy config backup
config.json
config.json.bak

# Frontend dependencies
frontend/node_modules/

# Frontend build output
frontend/dist/

# Tauri build artifacts
frontend/src-tauri/target/
frontend/src-tauri/gen/schemas/
# Tauri sidecar (Go server binary, built by make.ps1)
frontend/src-tauri/binaries/
# SDE data cache created by Go backend during Tauri runs
frontend/src-tauri/data/
# SQLite DB created by Go backend during Tauri runs
frontend/src-tauri/*.db
frontend/src-tauri/*.db-shm
frontend/src-tauri/*.db-wal

# TypeScript build cache
frontend/tsconfig.tsbuildinfo

# Air live-reload config
.air.toml

# Claude settings
.claude/

# IDE / editor
.vscode/
.idea/
*.swp
*.swo
*~

# OS files
.DS_Store
Thumbs.db
desktop.ini

# Environment files
.out
.env
.env.local
.env.*.local
frontend/.env
frontend/.env.local
NUL
